
---

### 🧩 `0.6_new_note_spa_diagram.md`

```markdown
```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: User types a note and clicks "Save"

    browser->>browser: JavaScript prevents form reload (preventDefault)
    browser->>browser: Adds new note to list and re-renders instantly
    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>browser: 201 Created (success)
    deactivate server

    Note right of browser: Browser stays on the same page and shows updated notes
